Note: *almost* all of the scripts can be run with the scripts/unitrans.db file, but our raw XML observations are stored in datasets/*.tar.gz as compressed archives (otherwise git will choke on the number of files). 
You will need to unarchive these first. You can use extract.py for this purpose.
